import 'package:flutter/widgets.dart';

class ConnectedCountryStatusArea extends StatelessWidget {
  const ConnectedCountryStatusArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
